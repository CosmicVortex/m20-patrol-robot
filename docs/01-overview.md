# 01 — 项目概览

## 目标

在云深处山猫 M20 Pro 机器狗上实现巡逻安防系统的二次开发，部署在 GOS（用户开发主机）上，通过 basic_server 协议与 AOS 通信，提供状态监控、视频回传和受控导航能力。

## 范围

**纳入范围：**
- M20 Pro 本体前后相机视频回传
- 实时状态监控（运动状态、电量、异常、定位）
- Web 人工单点导航控制（授权后）
- 办公室 → 门店两阶段部署

**不纳入范围：**
- AOS/NOS 原厂服务修改
- 数尔安防云台/视频适配（待后续独立开发）
- S10 平台适配（待办公室验收后评估）
- 16台统一管理（待后续阶段）

## 场地顺序

```
阶段1：华翔智行办公室
  ├─ 建图、定位核验
  ├─ 遥控器/APP 自主导航基线
  ├─ 状态接入、视频切换
  ├─ Web 单点导航控制
  └─ 验收通过后迁移

阶段2：东莞中升之星奔驰 4S 店
  ├─ 重新建图（不得复用办公室地图）
  ├─ 重新采集点位
  └─ 重新验收
```

办公室地图、地图包和点位不得直接带入门店；每个场地必须记录独立地图身份和整包 SHA-256。

## 当前阶段

**离线基线完成，等待办公室实机准入。**

|- 协议编解码、状态解析、导航报文构造、门禁系统全部离线验证通过
|- 19份官方文档已入库
|- 89个测试用例全部通过
|- 三个代码阻塞项已全部修复
|- 真实状态订阅（R-06）已完成并验证

## 关键安全规则

1. `control_enabled=false` 为默认值，真实连接必须显式启用
2. 导航发送前必须满足：型号确认、版本兼容、权限批准、真实样本、状态正常、书面放行
3. 所有地址、端口为候选值，必须现场签认
4. 模拟状态标注 `SIMULATED`，不得伪装为真实设备状态

## 下一步

1. 办公室现场取证：GOS/NOS/AOS 版本确认、basic_server 真实样本、RTSP 参数实测
2. 实现 TelemetryAdapter 连接真实 TCP 流
3. 实现 VideoProxy 完成 RTSP 拉流与 Web 展示
4. 书面放行后完成 Web 单点导航控制闭环

详见 [04-requirements.md](./04-requirements.md)。
