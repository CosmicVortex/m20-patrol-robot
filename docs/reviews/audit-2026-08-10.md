# M20 Pro 巡逻机器人 — 代码审查与修复报告

**审查日期**: 2026-08-10
**审查范围**: backend/ 全量 Python 代码
**测试状态**: 180 passed

---

## 第一部分：代码审查结果

### P0 — 必须修复

| 编号 | 问题 | 位置 | 影响 |
|------|------|------|------|
| P0-1 | `VideoStreamManager.get_all_states()` 缺少 `label` 字段 | `backend/app/video/stream_manager.py:93-103` | 前端无法获取相机名称，仅显示 source key |
| P0-2 | `_calculate_coverage` 返回 float 但前端 `textContent = '0%'` 硬编码 | `backend/app/dashboard_realtime.py:553` | 覆盖率始终显示 0%，应为 `v.inspection_stats.coverage_rate` |

### P1 — 重要修复

| 编号 | 问题 | 位置 | 影响 |
|------|------|------|------|
| P1-1 | `GimbalConfig` 默认密码 `123456` 硬编码 | `backend/app/gimbal/adapter.py:25` | 安全隐患，应通过环境变量或 manifest 配置 |
| P1-2 | `VideoStreamManager` 默认 RTSP 地址硬编码为 `10.21.31.103` | `backend/app/video/stream_manager.py:50-52` | 候选值，需现场确认；应标注为候选 |
| P1-3 | `DevicesListHandler` 中 NOS 地址硬编码为 `10.21.31.106` | `backend/app/api/handlers.py:257` | 候选值，应从 manifest 读取 |
| P1-4 | `dashboard_realtime.py` 未使用 `TelemetryAdapter`，而是内置状态逻辑 | `backend/app/dashboard_realtime.py:113-200` | 功能重复，应统一使用 `TelemetryAdapter` |

### P2 — 改进项

| 编号 | 问题 | 位置 | 影响 |
|------|------|------|------|
| P2-1 | `set_rtsp_url` 使用 `_replace` 但 `CameraConfig` 非 frozen dataclass | `backend/app/video/stream_manager.py:94` | 运行时可行，但语义不清晰 |
| P2-2 | 部署脚本仍导入 `dashboard_realtime` 模块 | `deploy/scripts/deploy-readonly.sh:100` | 功能测试，不影响运行时 |
| P2-3 | `docs/03-modules.md` 描述过时 | `docs/03-modules.md` | 文档与代码不一致 |
| P2-4 | `GimbalAdapter` 默认 IP `192.168.1.108` 硬编码 | `backend/app/gimbal/adapter.py:244` | 候选值，应标注 |

---

## 第二部分：已执行的修复

### 修复 1: VideoStreamManager 添加 label 字段

**文件**: `backend/app/video/stream_manager.py`

```python
# 修改前
return {
    ...
    "rtsp_url": self._streams[source].rtsp_url,
}

# 修改后
return {
    ...
    "rtsp_url": self._streams[source].rtsp_url,
    "label": self._streams[source].name,
}
```

### 修复 2: 添加 set_rtsp_url 方法

**文件**: `backend/app/video/stream_manager.py`

新增 `set_rtsp_url(source, rtsp_url)` 方法，支持运行时更新 RTSP 地址。

### 修复 3: 覆盖率显示修复

**文件**: `backend/app/dashboard_realtime.py`

```javascript
// 修改前
metrics[1].querySelector('.num').textContent = '0%';

// 修改后
metrics[1].querySelector('.num').textContent = (v.inspection_stats?.coverage_rate ?? 0) + '%';
```

---

## 第三部分：待确认事项（需现场执行）

| 编号 | 待确认项 | 当前值 | 建议操作 |
|------|----------|--------|----------|
| C1 | AOS RTSP 地址 | `rtsp://10.21.31.103:8554/{video1,video2,thermal}` | 使用 ffprobe 确认可达性 |
| C2 | 云台默认 IP | `192.168.1.108` | 使用 `/api/v1/gimbal/scan` 扫描确认 |
| C3 | NOS 地址 | `10.21.31.106` | 确认为固定地址或候选值 |
| C4 | 云台默认密码 | `123456` | 修改为现场配置值 |
| C5 | Python 版本兼容性 | 当前运行 Python 3.13 | 需验证 Python 3.8.10 (GOS 目标版本) |

---

## 第四部分：测试结果

```bash
$ PYTHONPATH=. uv run --with pytest pytest -q --tb=short
........................................................................ [ 40%]
........................................................................ [ 80%]
....................................                                     [100%]
180 passed in 5.33s
```

**状态**: ✅ 所有测试通过，无回归

---

## 第五部分：数据一致性检查

### 字段命名对照

| 层 | 字段名 | 状态 |
|----|--------|------|
| 解析器 `status.py` | `motion_state`, `gait`, `roll`, `pitch`, `yaw` | ✅ snake_case |
| 遥测 `telemetry.py` | `basic`, `motion`, `device`, `errors`, `nav_status` | ✅ 一致 |
| API `handlers.py` | `sources.front.label`, `sources.rear.label` | ✅ 已修复 |
| 前端 JavaScript | `m.roll`, `b.gait` | ✅ 一致 |

### 配置项对照

| 配置项 | 默认值 | 使用位置 | 状态 |
|--------|--------|----------|------|
| `telemetry_tx_enabled` | `False` | `ConnectionConfig`, `WebServiceConfig` | ✅ 安全默认 |
| `control_enabled` | `False` | `BasicServerConfig`, `NavigationSafetySnapshot` | ✅ 安全默认 |
| `read_only_mode` | `True` | `WebServiceConfig`, `DashboardConfig` | ✅ 安全默认 |
| `allow_real_io` | `False` | `VideoStreamManager` | ✅ 安全默认 |
| `gimbal_password` | `"123456"` | `GimbalConfig` | ⚠️ 需修改 |

---

## 第六部分：安全门控验证

| 检查项 | 预期 | 实际 | 状态 |
|--------|------|------|------|
| `control_enabled` 默认值 | `False` | `False` | ✅ |
| `telemetry_tx_enabled` 默认值 | `False` | `False` | ✅ |
| 门禁检查存在 | `BasicServerClient.require_control_permission()` | ✅ | ✅ |
| 导航门禁检查 | `NavigationService.send_navigation()` | ✅ | ✅ |
| 紧急停止需 admin | `EmergencyStopHandler` | ✅ | ✅ |
| 遥测 TX 运行时强制关闭 | `ConnectionConfig.__post_init__` | ✅ | ✅ |

---

## 第七部分：代码质量统计

| 指标 | 数值 |
|------|------|
| Python 文件总数 | 26 |
| 测试文件总数 | 17 |
| 测试总数 | 180 |
| 编译错误 | 0 |
| 语法错误 | 0 |
| 已废弃但未删除模块 | `dashboard_realtime.py` (有引用) |

---

## 第八部分：后续建议

1. **P1-1**: 修改 `GimbalConfig` 默认密码，从环境变量 `M20_GIMBAL_PASSWORD` 读取
2. **P1-2**: 将 RTSP 地址改为从 manifest 或环境变量读取
3. **P1-3**: 从 manifest 读取 NOS 地址，避免硬编码
4. **P2-2**: 清理部署脚本中的 `dashboard_realtime` 导入，改用 `server` 模块
5. **长期**: 考虑废弃 `dashboard_realtime.py`，统一使用 `server.py` + `TelemetryAdapter`
