# M20 Pro 项目规范化整理方案

**日期**: 2026-08-15
**目标**: 清理冗余文件，优化目录结构，建立规范的项目管理架构

---

## 一、当前项目状态分析

### 1.1 文件统计

| 类别 | 数量 | 大小 | 状态 |
|------|------|------|------|
| Python源码 | 64 | 0.3 MB | ✅ 核心 |
| Markdown文档 | 42 | 1.2 MB | ⚠️ 需清理 |
| JavaScript | 12 | 0.1 MB | ✅ 核心 |
| Shell脚本 | 11 | 0.03 MB | ⚠️ 有冗余 |
| 归档文件 | 3 | 400 MB | 📦 待整理 |
| 测试文件 | 23 | 0.1 MB | ✅ 核心 |

### 1.2 发现的问题

| 问题类型 | 数量 | 影响 |
|----------|------|------|
| 重复诊断脚本 | 6个 → 保留1个 | 冗余代码 |
| 过程性报告文档 | 14个 | 仓库膨胀 |
| 调试脚本未清理 | 2个 | 无生产价值 |
| 缓存目录未排除 | 3个 | git跟踪 |
| 归档目录结构混乱 | 多处 | 管理困难 |

---

## 二、文件清理清单

### 2.1 删除（无保留价值）

| 文件/目录 | 原因 | 大小 |
|-----------|------|------|
| `archive/reports-2026-08-14/*.md` (14个) | 过程性文档 | 648 KB |
| `archive/reports-2026-08-14/*.zip` (2个) | 历史部署包 | - |
| `deploy/scripts/debug-*.sh` (2个) | 调试脚本，无生产价值 | 10 KB |
| `deploy/scripts/quick_diagnose.sh` | 与diagnose-gos.sh功能重叠 | 3 KB |
| `.pytest_cache/` | pytest缓存 | 23 KB |
| `.venv/` | Python虚拟环境（已在.gitignore） | 6 MB |
| `var/*.db-wal` | SQLite WAL文件 | 动态生成 |

### 2.2 保留（核心生产文件）

| 文件/目录 | 用途 |
|-----------|------|
| `backend/app/*.py` | 后端核心代码（34个文件） |
| `backend/tests/*.py` | 测试套件（23个文件） |
| `docs/项目文档/*.md` | 核心项目文档（6个文件） |
| `docs/官方文档/*.md` | 官方协议文档（19个文件） |
| `docs/website/*` | Web UI前端（17个文件） |
| `deploy/scripts/deploy-readonly.sh` | 主部署脚本 |
| `deploy/scripts/rollback-gos.sh` | 回滚脚本 |
| `deploy/scripts/build-offline-deploy-package.sh` | 离线包构建 |
| `deploy/scripts/diagnose-gos.sh` | GOS诊断（合并其他诊断功能） |
| `deploy/readonly-manifest.json` | 部署配置 |
| `deploy/systemd/*.service` | systemd服务定义 |
| `CHANGELOG.md` | 版本历史 |
| `LICENSE` | 开源许可证 |
| `README.md` | 项目说明 |
| `start.sh` | 本地启动脚本 |
| `build_deploy.py` | 部署包构建工具 |

### 2.3 归档（大文件或历史记录）

| 文件/目录 | 原因 | 目标位置 |
|-----------|------|----------|
| `archive/deploy/offline/ffmpeg/*.xz` | FFmpeg离线包(98MB) | `archive/releases/ffmpeg/` |
| `archive/m20-patrol-robot-deploy.tar.gz` | 历史部署包(5.6MB) | `archive/releases/deploy/` |
| `archive/deploy/*.tar.gz` | 历史离线包(110MB) | `archive/releases/deploy/` |
| `archive/docs/官方文档/*.pdf` | 官方手册PDF(6MB) | `archive/reference/manuals/` |

---

## 三、目标目录结构

```
m20-patrol-robot/
├── .git/                    # Git版本控制
├── .gitignore               # Git忽略规则
├── README.md                # 项目说明
├── CHANGELOG.md             # 版本历史
├── LICENSE                  # 开源许可证
├── start.sh                 # 本地启动脚本
├── build_deploy.py          # 部署包构建工具
│
├── backend/                 # 后端代码
│   ├── app/
│   │   ├── api/            # API处理器
│   │   ├── auth/           # 认证模块
│   │   ├── gimbal/         # 云台控制
│   │   ├── motion/         # 运动控制
│   │   ├── navigation/     # 导航控制
│   │   ├── protocol/       # APDU协议
│   │   ├── robot/          # 机器狗连接
│   │   ├── utils/          # 工具函数
│   │   ├── video/          # 视频管理
│   │   ├── websocket/      # WebSocket支持
│   │   ├── config.py       # 配置加载
│   │   └── server.py       # 服务入口
│   ├── tests/              # 测试套件
│   └── init_users.py       # 用户初始化
│
├── deploy/                  # 部署相关
│   ├── scripts/
│   │   ├── deploy-readonly.sh       # 主部署脚本
│   │   ├── rollback-gos.sh          # 回滚脚本
│   │   ├── build-offline-deploy-package.sh  # 离线包构建
│   │   └── diagnose-gos.sh          # GOS诊断（合并版）
│   ├── offline/
│   │   └── ffmpeg/
│   │       ├── install-ffmpeg-offline.sh
│   │       ├── OFFLINE_FFMPEG_INSTALL.md
│   │       └── SHA256SUMS           # FFmpeg校验文件
│   ├── systemd/
│   │   ├── m20-patrol-readonly.service
│   │   └── m20-patrol-simulated.service
│   ├── readonly-manifest.json
│   └── python-unzip.py
│
├── docs/                    # 文档
│   ├── website/             # Web UI前端
│   │   ├── index.html
│   │   ├── css/
│   │   ├── js/
│   │   └── images/
│   ├── 官方文档/
│   │   ├── 机器狗本体/      # 山猫官方协议文档
│   │   └── 上装设备/        # 数尔云台文档
│   └── 项目文档/            # 项目核心文档
│       ├── 01-需求分析.md
│       ├── 02-项目架构.md
│       ├── 03-模块说明.md
│       ├── 04-机器狗环境说明.md
│       ├── 05-部署说明.md
│       └── 06-演示方案.md
│
├── archive/                 # 归档目录
│   ├── releases/            # 历史发布包
│   │   ├── deploy/          # 历史部署包
│   │   └── ffmpeg/          # FFmpeg离线包
│   ├── reference/           # 参考文档
│   │   └── manuals/         # 官方手册PDF
│   └── misc-2026-08-14/     # 其他归档
│
└── var/                     # 运行时数据（可选）
    ├── m20_auth.db          # SQLite数据库
    ├── m20_auth.db-shm
    └── m20_auth.db-wal
```

---

## 四、执行计划

### 阶段一：清理（预计5分钟）

```bash
# 1. 删除缓存和临时文件
rm -rf .pytest_cache/ .venv/
rm -f var/*.db-wal

# 2. 删除冗余诊断脚本
rm deploy/scripts/debug-ffmpeg-check.sh
rm deploy/scripts/debug-ffmpeg-to-file.sh
rm deploy/scripts/quick_diagnose.sh
rm deploy/scripts/diagnose.sh
rm deploy/scripts/diagnose_gos_connection.sh

# 3. 删除过程性报告（保留关键参考）
rm archive/reports-2026-08-14/*.md
rm archive/reports-2026-08-14/*.zip
```

### 阶段二：重组（预计5分钟）

```bash
# 1. 创建归档子目录
mkdir -p archive/releases/{deploy,ffmpeg}
mkdir -p archive/reference/manuals

# 2. 移动归档文件
mv archive/deploy/offline/ffmpeg/*.xz archive/releases/ffmpeg/
mv archive/m20-patrol-robot-deploy.tar.gz archive/releases/deploy/
mv archive/deploy/*.tar.gz archive/releases/deploy/ 2>/dev/null || true
mv archive/docs/官方文档/*.pdf archive/reference/manuals/ 2>/dev/null || true

# 3. 删除空目录
rmdir archive/deploy/offline/ffmpeg archive/docs/官方文档/机器狗本体 2>/dev/null || true
```

### 阶段三：优化.gitignore

```gitignore
# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/

# Virtual Environment
.venv/
venv/
env/

# pytest
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 数据库文件
var/*.db-wal
var/*.db-shm

# 临时文件
*.tmp
*.bak

# 大文件（已在archive中）
!archive/releases/
!archive/reference/
*.tar.gz
!archive/releases/deploy/*.tar.gz
*.tar.xz
!archive/releases/ffmpeg/*.tar.xz
```

### 阶段四：验证

```bash
# 运行测试
PYTHONPATH=. uv run --with pytest pytest backend/tests/ -q

# 检查git状态
git status --short

# 查看文件大小变化
du -sh . archive/ docs/ backend/
```

---

## 五、预期效果

| 指标 | 清理前 | 清理后 | 变化 |
|------|--------|--------|------|
| 仓库大小 | ~400 MB | < 5 MB | -98% |
| 过程性文档 | 20个 | 0个 | 全部清理 |
| 冗余脚本 | 6个 | 1个 | -83% |
| 缓存目录 | 3个 | 0个 | 100%清理 |
| 核心文件完整性 | - | 100%保留 | - |

---

## 六、后续维护规范

### 6.1 文档管理规范

- 核心文档：`docs/项目文档/` 编号01-06
- 官方文档：`docs/官方文档/` 按设备分类
- 历史归档：`archive/` 按类型和时间分类
- 禁止在仓库根目录存放过程性报告

### 6.2 脚本管理规范

- 生产脚本：`deploy/scripts/`（保留核心4个）
- 调试脚本：开发完成后删除
- 诊断脚本：合并为单一脚本，功能模块化

### 6.3 归档管理规范

- 历史部署包：`archive/releases/deploy/`
- 官方手册PDF：`archive/reference/manuals/`
- FFmpeg离线包：`archive/releases/ffmpeg/`
- 归档时生成SHA256校验文件

---

**方案制定**: Agnes AI Assistant
**审核状态**: 待项目负责人确认
