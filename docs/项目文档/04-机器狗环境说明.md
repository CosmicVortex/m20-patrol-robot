# 机器狗环境说明

## 1. 系统组成

| 节点 | 地址 | 角色 | 系统 |
|------|------|------|------|
| GOS | 10.21.31.104 | 边缘计算主机，运行Web服务 | Ubuntu 20.04.6 LTS (aarch64) |
| AOS | 10.21.31.103 | 机器狗本体控制系统 | 嵌入式Linux |
| NOS | 10.21.31.106 | 导航操作员站 | - |
| 数尔云台 | 10.21.31.108 | 上装设备控制器 | - |

> **注**: 以上地址为项目负责人确认的固定配置，不得修改。

## 2. 网络拓扑

```
                    ┌─────────────────┐
                    │   监控终端      │
                    │  (浏览器)       │
                    └────────┬────────┘
                             │ HTTP :8080
                    ┌────────▼────────┐
                    │    GOS 主机     │
                    │  10.21.31.104   │
                    │                 │
                    │  Python服务     │
                    │  FFmpeg         │
                    │  SQLite         │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
        TCP:30001       RTSP:8554     HTTP:80
              │              │              │
    ┌─────────▼────────┐     │        ┌────▼───────┐
    │    AOS 主机      │     │        │  数尔云台  │
    │  10.21.31.103    │     │        │10.21.31.108│
    │                  │     │        └────────────┘
    │  basic_server    │
    │  RTSP Server     │
    └─────────┬────────┘
              │
    ┌─────────▼────────┐
    │   M20 Pro 机器狗  │
    │   (运动+传感器)   │
    └──────────────────┘
    
    ┌──────────────────┐
    │    NOS 导航站    │
    │  10.21.31.106    │
    │  (通过AOS间接调用)│
    └──────────────────┘
```

## 3. basic_server协议要点

### 3.1 端口分配

| 协议 | 端口 | 用途 |
|------|------|------|
| TCP | 30001 | 长连接，状态订阅+控制指令 |
| UDP | 30000 | 高频指令（推荐） |

### 3.2 消息格式

**APDU头部 (16字节)**:
```
| Length(2) | SeqNum(2) | Type(2) | Command(2) | Reserved(8) |
```

**ASDU载荷 (JSON)**:
```json
{
  "PatrolDevice": {
    "Type": 1002,
    "Command": 6,
    "Time": "2026-08-14 00:00:00",
    "Items": { ... }
  }
}
```

### 3.3 关键Type/Command组合

| Type | Command | 方向 | 说明 |
|------|---------|------|------|
| 100 | 100 | 出 | 心跳请求 |
| 1002 | 3 | 入 | 错误列表 |
| 1002 | 4 | 入 | 运动状态 |
| 1002 | 5 | 入 | 设备状态 |
| 1002 | 6 | 入 | 基础状态 |
| 1003 | 1 | 出 | 导航任务 |
| 1004 | 1 | 出 | 取消导航 |
| 1007 | 1 | 入 | 导航状态 |
| 1007 | 2 | 入 | 位置信息 |
| 2002 | 1 | 入 | 感知状态 |
| 2 | 21 | 出 | 轴控制 |
| 2 | 22 | 出 | 状态转换 |
| 2 | 23 | 出 | 步态切换 |
| 2 | 24 | 出 | 充电控制 |

### 3.4 运动状态定义

| 状态值 | 名称 | 说明 |
|--------|------|------|
| 0 | 空闲 | 等待指令 |
| 1 | 站立 | 临时过渡状态 |
| 2 | 软急停 | 最高优先级 |
| 3 | 开机阻尼 | 启动后自动进入 |
| 4 | 趴下 | 安全趴下 |
| 17 | RL控制 | 可执行移动和步态切换 |

## 4. 常用诊断命令

### 4.1 GOS主机诊断

```bash
# 检查服务状态
systemctl --user status m20-patrol-readonly.service

# 查看服务日志
journalctl --user -u m20-patrol-readonly.service -f

# 健康检查
curl http://localhost:8080/api/v1/health

# 查看状态数据
curl http://localhost:8080/api/v1/status/latest | jq .

# 检查Python进程
ps aux | grep server.py

# 检查端口监听
ss -tlnp | grep 8080
```

### 4.2 网络连通性测试

```bash
# 测试AOS连接
nc -zv 10.21.31.103 30001
nc -zu 10.21.31.103 30000

# 测试NOS连接
nc -zv 10.21.31.106 22

# 测试云台连接
curl -s http://10.21.31.108/merlin/Login.cgi
ping -c 3 10.21.31.108
```

### 4.3 FFmpeg验证

```bash
# 检查FFmpeg安装
ffmpeg -version

# 探测RTSP流
ffprobe -v error -show_entries stream=codec_name,width,height,r_frame_rate -of json rtsp://10.21.31.103:8554/video1

# 测试转码
ffmpeg -i rtsp://10.21.31.103:8554/video1 -c:v libx264 -preset ultrafast -f buffer /dev/null
```

### 4.4 基本服务器通信测试

```bash
# 发送心跳测试
echo '{"PatrolDevice":{"Type":100,"Command":100,"Time":"2026-08-14 00:00:00","Items":{}}}' | nc 10.21.31.103 30001

# 查看服务配置
cat ~/.config/m20-patrol/passwords.env
cat ~/m20-patrol-robot/deploy/readonly-manifest.json
```

## 5. 已验证内容

| 项目 | 验证结果 | 验证时间 | 验证人 |
|------|----------|----------|--------|
| GOS服务启动 | ✅ 正常 | 2026-08-11 | 项目负责人 |
| 健康检查API | ✅ 返回healthy | 2026-08-11 | 项目负责人 |
| Python 3.8兼容 | ✅ 已验证 | 2026-08-14 | 云端编译检查 |
| FFmpeg安装 | ⚠️ 待现场验证 | - | - |
| AOS连接 | ⚠️ 待现场验证 | - | - |
| 数尔云台连通 | ⚠️ 待现场验证 | - | - |
| RTSP视频流 | ⚠️ 待现场验证 | - | - |

## 6. 待验证内容

### 6.1 必须验证（部署前）

```bash
# 1. 确认GOS架构
uname -m
# 期望: aarch64 或 arm64

# 2. 确认Python版本
python3 --version
# 期望: Python 3.8.10

# 3. 检查FFmpeg
command -v ffmpeg || true
ffmpeg -version 2>/dev/null | sed -n '1,2p' || true

# 4. 测试网络连通性
nc -zv 10.21.31.103 30001
curl -s http://10.21.31.104:8080/api/v1/health
```

### 6.2 可选验证（演示前）

```bash
# 5. 测试云台连接
curl -s -u admin:<your_password> http://10.21.31.108/merlin/GetFlyStateInfo.cgi

# 6. 测试RTSP流
ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 rtsp://10.21.31.103:8554/video1

# 7. 测试运动控制（需授权）
curl -X POST http://10.21.31.104:8080/api/v1/motion/authorize \
  -H "Content-Type: application/json" \
  -d '{"operator":"admin"}'
```

## 7. 服务管理

### 7.1 启动服务

```bash
# 一键部署
bash deploy/scripts/deploy-readonly.sh --one-shot

# 或手动启动
cd ~/m20-patrol-robot
PYTHONPATH=. python3 -m backend.app.server --manifest deploy/readonly-manifest.json
```

### 7.2 停止服务

```bash
systemctl --user stop m20-patrol-readonly.service
```

### 7.3 重启服务

```bash
systemctl --user restart m20-patrol-readonly.service
```

### 7.4 查看日志

```bash
# 实时日志
journalctl --user -u m20-patrol-readonly.service -f

# 历史日志
journalctl --user -u m20-patrol-readonly.service -n 100
```

## 8. 环境文件

密码配置文件位置: `~/.config/m20-patrol/passwords.env`

```bash
# 格式
M20_GIMBAL_PASSWORD=your_gimbal_password
# 默认密码: 见部署配置文件（首次部署后必须修改）
```
