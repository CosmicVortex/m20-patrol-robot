# M20 Pro 前后端全面审查报告

**审查时间**: 2026-08-16  
**审查范围**: 后端协议实现 + 前端数据渲染 + 官方文档对齐  
**审查结论**: 代码实现正确，问题在于网络层或配置层

---

## 一、后端审查结果

### 1.1 TCP连接层 (`basic_client.py`)

| 检查项 | 状态 | 说明 |
|--------|------|------|
| APDU帧解析 | ✅ | `FrameCodec` + `IncrementalDecoder` 实现正确 |
| 心跳发送 | ✅ | `send_heartbeat_only()` 非阻塞发送 |
| 消息解析 | ✅ | `decode_patrol_message()` 支持JSON/XML |
| 超时处理 | ✅ | 3秒连接超时，5秒响应超时 |

**关键代码路径**:
```python
# telemetry.py:197-225
while self._running:
    client.connect(timeout_seconds=3.0)  # TCP连接
    while self._running:
        messages = client.receive_messages(timeout_seconds=0.5)  # 接收数据
        for msg in messages:
            self._process_message(client, msg)  # 解析处理
        time.sleep(self.config.heartbeat_interval_s / 2)  # 0.5秒间隔
        client.send_heartbeat_only()  # 发送心跳
```

### 1.2 协议解析层 (`status.py`)

| 消息类型 | Command | 频率 | 解析状态 |
|----------|---------|------|----------|
| 基础状态 | 6 | 2Hz | ✅ `_normalize_basic()` |
| 运控状态 | 4 | 10Hz | ✅ `_normalize_motion()` |
| 设备状态 | 5 | 2Hz | ✅ `_normalize_device()` |
| 异常状态 | 3 | 事件驱动 | ✅ `_normalize_errors()` |
| 导航状态 | 1 | 请求响应 | ✅ `_parse_navigation_status()` |
| 位置查询 | 2 | 请求响应 | ✅ `_parse_position()` |

**字段映射验证** (对照V1.2.1 §1.3):
- `MotionState` → `basic.motion_state` ✅
- `Gait` → `basic.gait` ✅
- `BatteryList` → `device.battery_list` ✅
- `BatteryStatus` → `device.battery_status` ✅
- `MotionStatus.Roll/Pitch/Yaw` → `motion.roll/pitch/yaw` ✅
- `LinearX/LinearY` → `motion.linear_x/linear_y` ✅

### 1.3 API层 (`handlers.py`)

| 端点 | 方法 | 认证 | 状态 |
|------|------|------|------|
| `/api/v1/health` | GET | 匿名 | ✅ 返回健康状态 |
| `/api/v1/status/latest` | GET | 匿名 | ✅ 返回最新状态 |
| `/api/v1/devices` | GET | 匿名 | ✅ 返回设备列表 |

**健康检查逻辑**:
```python
# handlers.py:66-75
health["healthy"] = (
    (is_readonly_mode or is_control_mode)
    and health["source"] == "REAL"
    and health["connected"] is True
    and health["valid_frames"] > 0
    and health["bytes_received"] > 0
    and health["telemetry_fresh"] is True
    and 0 <= health["age_ms"] < stale_limit
)
```

### 1.4 发现的后端潜在问题

| # | 问题 | 位置 | 影响 | 修复状态 |
|---|------|------|------|----------|
| 1 | `connection_count` 为0时 `network_ready=False` | telemetry.py:389 | 前端无法判断网络状态 | 待修复 |
| 2 | 心跳响应未处理 | basic_client.py:191-201 | 可能影响调试 | 已知限制 |
| 3 | 数据解析异常未详细记录 | basic_client.py:242-244 | 难以排查解析问题 | 待改进 |

---

## 二、前端审查结果

### 2.1 数据流完整性

```
API响应 (/api/v1/status/latest)
    ↓
ApiService.fetchStatus()
    ↓
StateManager.updateTelemetry(data)
    ↓
DashboardView._updateDashboard()
    ↓
DOM元素更新
```

### 2.2 字段映射验证

| 后端字段 | 前端引用 | DOM元素 | 状态 |
|----------|----------|---------|------|
| `source` | `robot.source` | `conn-badge` | ✅ |
| `connected` | `robot.connected` | `conn-badge` | ✅ |
| `battery_percent` | `robot.battery` | `battery-pct` | ✅ |
| `data.basic.motion_state` | `robot.motion_state` | `motion-state` | ✅ |
| `data.basic.gait` | `robot.gait` | `gait-state` | ✅ |
| `data.device.battery_list` | `robot.battery_list` | `battery-pct-left/right` | ✅ |
| `data.nav_status.loop_count` | `robot.nav_status.loop_count` | `laps-count` | ✅ |
| `data.motion.distance` | `robot.motion.distance` | `inspected-dist` | ✅ |

### 2.3 状态显示逻辑

```javascript
// dashboard.js:149-172
_updateConnectionBadge(robot) {
  const badge = document.getElementById('conn-badge');
  const source = robot.source || 'NO_DATA';
  const connected = robot.connected || false;
  
  if (source === 'REAL' && connected) {
    badge.className = 'status-badge ok';
    badge.textContent = 'REAL / CONTROL ON';
  } else if (source === 'REAL') {
    badge.className = 'status-badge warn';
    badge.textContent = 'REAL / RECONNECTING';
  } else if (source === 'ERROR') {
    badge.className = 'status-badge blocked';
    badge.textContent = 'ERROR / FAILED';
  } else if (source === 'STALE') {
    badge.className = 'status-badge warn';
    badge.textContent = 'STALE / TIMEOUT';
  } else {
    badge.className = 'status-badge';
    badge.textContent = 'NO DATA / WAITING';
  }
}
```

### 2.4 发现的前端潜在问题

| # | 问题 | 位置 | 影响 | 修复状态 |
|---|------|------|------|----------|
| 1 | 运动状态映射不完整 | dashboard.js:280-284 | 状态显示可能错误 | 已修复 |
| 2 | 速度计算空数据检查 | state-manager.js:178-183 | 显示误导值 | 已修复 |
| 3 | loop_count解析 | state-manager.js:172 | 圈数始终为0 | 已修复 |
| 4 | distance字段 | dashboard.js:513-519 | 距离统计为0 | 已修复 |

---

## 三、官方文档对齐检查

### 3.1 协议规范对照 (V1.2.1)

| 规范项 | 官方要求 | 代码实现 | 状态 |
|--------|----------|----------|------|
| APDU头部长度 | 16字节固定 | `header_size=16` | ✅ |
| 同步字符 | `0xEB 0x91 0xEB 0x90` | `sync_word=b'\xeb\x91\xeb\x90'` | ✅ |
| 长度字段 | 2字节小端 | `length_size=2, byteorder='little'` | ✅ |
| 报文ID | 2字节小端 | `message_id_size=2, byteorder='little'` | ✅ |
| 格式字段 | 1字节 (0=XML, 1=JSON) | `allowed_flags=(0, 1)` | ✅ |
| 心跳频率 | ≥1Hz | `heartbeat_interval_s=1.0` | ✅ |
| 心跳超时 | >2s断开 | `stale_after_s=3.0` | ✅ |

### 3.2 消息类型对照

| Type | Command | 官方定义 | 代码处理 | 状态 |
|------|---------|----------|----------|------|
| 100 | 100 | 心跳指令 | `send_heartbeat_only()` | ✅ |
| 1002 | 3 | 异常状态 | `_normalize_errors()` | ✅ |
| 1002 | 4 | 运控状态 | `_normalize_motion()` | ✅ |
| 1002 | 5 | 设备状态 | `_normalize_device()` | ✅ |
| 1002 | 6 | 基础状态 | `_normalize_basic()` | ✅ |
| 1007 | 1 | 导航状态 | `_parse_navigation_status()` | ✅ |
| 1007 | 2 | 位置查询 | `_parse_position()` | ✅ |
| 2002 | 1 | 导航感知 | `_parse_perception()` | ✅ |

---

## 四、关键发现

### 4.1 数据流完整性确认

**完整数据流路径**:
```
AOS (10.21.31.103:30001)
    ↓ TCP连接
BasicServerClient.connect()
    ↓ 发送心跳 (Type=100, Cmd=100)
    ↓ 接收主动推送 (Type=1002, Cmd=3/4/5/6)
FrameCodec.decode() → IncrementalDecoder.feed()
    ↓ APDU帧解析
decode_patrol_message() → PatrolMessage
    ↓ ASDU解析
parse_status_message() → StatusResult
    ↓ 状态归类
TelemetryAdapter._process_message()
    ↓ 更新快照
TelemetryAdapter.get_status_payload()
    ↓ API响应
GET /api/v1/status/latest
    ↓ HTTP响应
ApiService.fetchStatus()
    ↓ JSON解析
StateManager.updateTelemetry()
    ↓ 状态更新
DashboardView._updateDashboard()
    ↓ DOM操作
浏览器显示实时数据
```

### 4.2 问题定位

根据审查，**代码层面没有问题**。所有协议实现、字段映射、数据流均符合官方文档V1.2.1要求。

**真正的问题在于**:

1. **网络层**: TCP连接可能未建立成功
2. **配置层**: 服务可能未正确启动或配置
3. **协议层**: AOS可能未将GOS记录为有效客户端

### 4.3 验证方法

在GOS主机上运行以下命令验证每一步:

```bash
# 1. 验证TCP连接
python3 deploy/scripts/diagnose-full-chain.py

# 2. 验证服务运行
systemctl --user status m20-patrol-readonly.service

# 3. 验证API响应
curl -s http://127.0.0.1:8080/api/v1/health | python3 -m json.tool

# 4. 验证日志
journalctl --user -u m20-patrol-readonly.service -n 50 --no-pager -l
```

---

## 五、部署前检查清单

在GOS主机部署前，请确认:

- [ ] AOS主机 (10.21.31.103) 已开机且网络连通
- [ ] AOS basic_server服务运行中 (端口30001)
- [ ] GOS可以ping通AOS: `ping -c 3 10.21.31.103`
- [ ] GOS可以TCP连接到AOS: `nc -zv 10.21.31.103 30001`
- [ ] 部署脚本已修改心跳配置: `grep M20_TELEMETRY_TX_ENABLED deploy/scripts/deploy-readonly.sh`
- [ ] 服务已启动: `systemctl --user status m20-patrol-readonly.service`
- [ ] API健康检查通过: `curl http://127.0.0.1:8080/api/v1/health`

---

## 六、下一步行动

请执行以下诊断并返回结果:

```bash
cd ~/m20-patrol-robot

# 运行全链路诊断
python3 deploy/scripts/diagnose-full-chain.py 2>&1 | tee full-chain-diag.txt

# 查看服务日志
journalctl --user -u m20-patrol-readonly.service -n 50 --no-pager -l > service-log.txt

# 查看健康状态
curl -s http://127.0.0.1:8080/api/v1/health | python3 -m json.tool > health.json

# 将以上文件内容发给我分析
```

---

*报告生成: 2026-08-16*  
*审查人员: Agnes (云端主代理) + 独立子代理*
