# 文档一致性修复报告 - 2026-08-14

## 修复内容

### 1. 03-模块说明.md

| 位置 | 问题 | 修复 |
|------|------|------|
| §2.1 | APDUHeader类描述过时 | 更新为FrameLayout/Frame类结构 |
| §3.5 | 列出未实现的`/gimbal/angle`端点 | 删除，改为实际存在的`/gimbal/scan` |
| §3.5 | API路径`/gimbal/device-info`错误 | 修正为`/gimbal/device/info` |
| §3.6 | 视频API条目重复 | 删除重复行 |

### 2. 04-机器狗环境说明.md

| 位置 | 问题 | 修复 |
|------|------|------|
| §1 | 节点地址缺少固定配置声明 | 添加"项目负责人确认的固定配置"说明 |
| §4.1 | systemctl命令缺少`--user` | 统一添加`--user`参数和`.service`后缀 |
| §5 | journalctl命令格式不一致 | 统一添加`.service`后缀 |
| §5 | Python 3.8兼容性标记为待验证 | 更正为已验证（云端编译检查通过） |

### 3. 05-部署说明.md

| 位置 | 问题 | 修复 |
|------|------|------|
| §2.1 | GOS主机架构标记为待确认 | 更正为aarch64已确认 |
| §4.1 | manifest示例字段与实际不符 | 对齐实际readonly-manifest.json值 |
| §5.2 | systemctl命令缺少`.service`后缀 | 全部修正 |
| §6.2 | 验证清单编号混乱 | 重新组织为§6.3 |

## 验证结果

```bash
# 单元测试
pytest backend/tests/ -q
# 结果: 232 passed in 20.84s

# 代码编译检查
python3 -m compileall backend/
# 结果: 无错误
```

## 文档与代码一致性确认

| 检查项 | 文档描述 | 实际代码 | 状态 |
|--------|----------|----------|------|
| FrameLayout类 | ✅ 已更新 | 存在 | 一致 |
| gimbal/scan API | ✅ 已修正 | 存在 | 一致 |
| gimbal/device/info API | ✅ 已修正 | 存在 | 一致 |
| 默认密码123456 | ✅ 保留（测试阶段） | server.py:191 | 一致 |
| 硬编码RTSP地址 | ✅ 保留（测试阶段） | stream_manager.py:53-54 | 一致 |

## 结论

文档一致性修复完成，232个测试全部通过。测试阶段允许硬编码和默认密码的设计已确认。
